[project]
name = "lobsim"
version = "0.1.0"
description = "Limit Order Book Simulator"
dependencies = [
    "pybind11>=2.11.1",
    "numpy>=1.26.0",
]
requires-python = ">=3.11"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

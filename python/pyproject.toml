[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta:__legacy__"

[project]
name = "lobsim"
version = "0.1.0"
description = "LOBSIM Python bindings"
authors = [{ name = "LOBSIM", email = "devnull@example.com" }]
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta:__legacy__"

[project]
name = "simex"
version = "0.1.0"
description = "SIMEX Python bindings"
authors = [{ name = "SIMEX", email = "devnull@example.com" }]
requires-python = ">=3.11"
